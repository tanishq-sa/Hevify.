/**
 * Workout-related constants and default values
 */

// Default rest timer in seconds
export const DEFAULT_REST_TIMER = 50;

// Available rest timer options (in seconds)
export const REST_TIMER_OPTIONS = [
  30, 35, 40, 45, 50, 55, 60, 65, 70, 75, 80, 90, 120, 150, 180
];

// Default clock timer value (in seconds)
export const DEFAULT_CLOCK_TIMER = 60;

// Maximum rest timer value (in seconds) - 1 hour
export const MAX_REST_TIMER = 3600;

// Maximum clock timer value (in seconds) - 1 hour
export const MAX_CLOCK_TIMER = 3600;

